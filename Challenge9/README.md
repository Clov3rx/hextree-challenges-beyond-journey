# Flag 9 - Returning Activity Results Data

## Solution 

1. `(callingActivity.getClassName().contains("Hextree"))` we can bypass this by create a new package containing Hextree 
2.  The Flag9Activity will send the flag back to you , so reciving data by `onActivityResult` Method 

